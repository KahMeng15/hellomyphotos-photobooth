NODE_ENV=production
PORT=3000
JWT_SECRET=change-me-to-a-strong-random-string
REFRESH_TOKEN_SECRET=change-me-to-another-strong-random-string
OPERATOR_EMAIL=operator@hellomyphoto.local
OPERATOR_PASSWORD=admin123
ALLOWED_ORIGINS=http://localhost:3000,http://192.168.1.*

# Security & Cookie Settings
# Domain name for cookies (e.g., example.com) - Leave empty for localhost
COOKIE_DOMAIN=example.com
# Base path if the server is hosted in a subfolder (e.g., /hellomyphotos-photobooth)
COOKIE_PATH=/hellomyphotos-photobooth
# SameSite policy: strict (default), lax, or none
COOKIE_SAME_SITE=strict
# Force secure cookies (HTTPS required) if not automatically handled by NODE_ENV=production
COOKIE_SECURE=true
